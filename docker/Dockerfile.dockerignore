# The source tree is copied into the image, so the context is the repository. What is excluded is
# what would only make it heavier: history, build output, and anything Gradle regenerates.
.git
**/.git
**/build
**/.gradle
cachedModules
docker/*.png
